export interface VCardProps {
  image: string;
  title: string;
  description: string;
  questionType?: string;
}

export type CardProps = {
  title?: string;
  description?: string;
  image: string;
};
